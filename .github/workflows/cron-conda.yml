name: "Conda Update"

on:
  schedule:
    - cron: '0 10 * * 6' # everyday at 10am
  push:
    branches: [master, ]

jobs:
  conda-update:
    name: Update requirements.txt
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - uses: actions/setup-python@v7
        with:
          python-version: '3.14'

      - name: Install and Setup
        run: |
          set -x

          python -m pip install -U pip setuptools wheel packaging
          python -m pip install -e .
          python -m pip install pysqlite3 PyMySQL[rsa] psycopg2 dataclasses

      - name: Setup Conda Distribution
        run: |
          export DARC_DEVMODE=1
          export DARC_VERBOSE=1

          python util/conda-dist.py

      - name: Verify Changed files
        uses: tj-actions/verify-changed-files@v17
        id: verify-changed-files

      - name: Bump Version
        if: steps.verify-changed-files.outputs.files_changed == 'true'
        run: |
          python util/conda-build.py

      - name: Commit Changes
        if: steps.verify-changed-files.outputs.files_changed == 'true'
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git commit -am"Bumped build to $(cat conda/build)

          Updated conda distribution at $(date). The following files have been changed:
            ${{ steps.verify-changed-files.outputs.changed_files }}"

      - name: Push Changes
        uses: ad-m/github-push-action@master
        if: steps.verify-changed-files.outputs.files_changed == 'true'
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          branch: ${{ github.ref }}

      - name: Get Version
        if: steps.verify-changed-files.outputs.files_changed == 'true'
        id: get_version
        run: |
          set -ex

          export DARC_USER="nobody"
          export TOR_PASS="null"

          echo "DARC_VERSION=$(python -c 'import darc; print(darc.__version__)')" >> $GITHUB_OUTPUT
          echo "DARC_CONDA_LABEL=$(python -c 'from packaging.version import Version; import darc; print("dev" if Version(darc.__version__).is_prerelease else "main")')" >> $GITHUB_OUTPUT
          echo "DARC_BUILD=$(cat conda/build)" >> $GITHUB_OUTPUT

    outputs:
      CONDA_CHANGED: ${{ steps.verify-changed-files.outputs.files_changed }}
      DARC_VERSION: ${{ steps.get_version.outputs.DARC_VERSION }}
      DARC_CONDA_LABEL: ${{ steps.get_version.outputs.DARC_CONDA_LABEL }}
      DARC_BUILD: ${{ steps.get_version.outputs.DARC_BUILD }}

  conda-tag:
    name: Conda Tag
    runs-on: ubuntu-latest
    needs: [ conda-update ]
    if: needs.conda-update.outputs.CONDA_CHANGED == 'true'
    permissions:
      contents: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v7
        with:
          token: ${{ secrets.DARC }}
          fetch-depth: 0

      - name: Create Tag
        run: |
          set -x

          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"

          git pull
          git tag "conda-${{ needs.conda-update.outputs.DARC_VERSION }}+${{ needs.conda-update.outputs.DARC_BUILD }}" -m"Conda Build"

      - name: Push Tag
        uses: ad-m/github-push-action@master
        with:
          github_token: ${{ secrets.DARC }}
          atomic: false
          tags: true

  conda-dist:
    name: Conda deployment of package for platform ${{ matrix.os }} with Python ${{ matrix.python-version }}
    runs-on: ${{ matrix.os }}
    needs: [ conda-tag, conda-update ]
    if: needs.conda-update.outputs.CONDA_CHANGED == 'true'
    permissions:
      contents: write
    strategy:
      matrix:
        os:
          #- macos-latest
          - ubuntu-latest
          # - windows-latest  # skip for now
        python-version:
          - "3.14"
    steps:
      - uses: actions/checkout@v7
        with:
          ref: conda-${{ needs.conda-update.outputs.DARC_VERSION }}+${{ needs.conda-update.outputs.DARC_BUILD }}
          fetch-depth: 0

      - uses: actions/setup-python@v7
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install and Setup
        if: ${{ matrix.os != 'windows-latest' }}
        run: |
          set -x

          python -m pip install -U pip setuptools wheel
          python -m pip install -e .
          python -m pip install pysqlite3 PyMySQL[rsa] psycopg2 dataclasses

      - name: Install and Setup (Windows)
        if: ${{ matrix.os == 'windows-latest' }}
        run: |
          python -m pip install -U pip setuptools wheel
          python -m pip install -e .
          python -m pip install pysqlite3 PyMySQL[rsa] psycopg2 dataclasses

      - name: Setup Conda Distribution
        if: ${{ matrix.os != 'windows-latest' }}
        run: |
          set -x

          mkdir -p conda/wheels
          python -m pip download -r conda/requirements.txt -d conda/wheels

      - name: Setup Conda Distribution (Windows)
        if: ${{ matrix.os == 'windows-latest' }}
        run: |
          mkdir -p conda/wheels
          python -m pip download -r conda/requirements.txt -d conda/wheels

      - name: Conda environment creation and activation
        uses: conda-incubator/setup-miniconda@v4.0.1
        with:
          python-version: ${{ matrix.python-version }}
          activate-environment: base
          auto-update-conda: false
          auto-activate: true
          channels: conda-forge,defaults
          conda-remove-defaults: false
          show-channel-urls: true

      - name: Install conda build tools
        run: |
          conda install -n base --override-channels -c conda-forge -y conda-build
          conda build --version

      - name: Build conda packages
        id: conda_build
        shell: bash -el {0}
        env:
          DARC_VERSION: ${{ needs.conda-update.outputs.DARC_VERSION }}
          DARC_BUILD: ${{ needs.conda-update.outputs.DARC_BUILD }}
        working-directory: conda/darc
        run: |
          set -euo pipefail

          out_dir="$(mktemp -d -t compilation-XXXXXXXXXX)"
          echo "out_dir=$out_dir" >> "$GITHUB_OUTPUT"
          conda build . --package-format .tar.bz2 --no-anaconda-upload --output-folder "$out_dir" --python "${{ matrix.python-version }}"
          package_paths="$(find "$out_dir" -type f \( -name '*.tar.bz2' -o -name '*.conda' \) -print | sort)"
          if [ -z "$package_paths" ]; then
            echo "::error::No conda package was built"
            exit 1
          fi
          printf '%s\n' "$package_paths"
          package_paths_csv="$(printf '%s\n' "$package_paths" | paste -sd ',' -)"
          echo "packages_csv=$package_paths_csv" >> "$GITHUB_OUTPUT"
          {
            echo 'packages<<EOF'
            printf '%s\n' "$package_paths"
            echo EOF
          } >> "$GITHUB_OUTPUT"

      - name: Upload conda packages to Anaconda
        uses: anaconda/actions/upload-package@v0.3.1
        with:
          token: ${{ secrets.ANACONDA_TOKEN }}
          channel: jarryshaw
          packages: ${{ steps.conda_build.outputs.packages }}
          labels: ${{ needs.conda-update.outputs.DARC_CONDA_LABEL }}
          force: true

      - name: Upload package to GitHub Release
        if: ${{ matrix.os == 'ubuntu-latest' }}
        uses: ncipollo/release-action@v1.21.0
        with:
          allowUpdates: true
          artifacts: ${{ steps.conda_build.outputs.packages_csv }}
          tag: "v${{ needs.conda-update.outputs.DARC_VERSION }}"
          token: "${{ secrets.GITHUB_TOKEN }}"
